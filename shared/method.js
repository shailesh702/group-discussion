Meteor.methods({
	addPost : function(msg){
		//const text = event.target.commentbox.value;		
 		//alert(msg);
		event.target.commentbox.value='';
		//event.target.commentbox.value=msg + " buddy";
		//event.target.text2.value = msg;
		//event.target.showcommentbox.value="hello";
		//event.target.show.value=text;
		
	}
});

